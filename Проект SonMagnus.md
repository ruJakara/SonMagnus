# Проект SonMagnus

Добро пожаловать в репозиторий **SonMagnus**, инди-игры, разрабатываемой на движке Godot Engine 4.5.1.

## 🛠️ Технологии

*   **Игровой движок:** Godot Engine 4.5.1
*   **Язык программирования:** GDScript / C# (указать нужное)
*   **Система контроля версий:** Git

## 🌳 Структура Ветвления (Git Flow)

Мы используем модифицированный подход Git Flow для обеспечения стабильности и эффективной разработки.

| Ветка | Назначение | Описание |
| :--- | :--- | :--- |
| `main` | **Стабильные сборки (Production)** | Содержит только протестированные, готовые к релизу или публичному демо-версии. Коммиты попадают сюда только через слияние из `develop` или `hotfix`. |
| `develop` | **Текущая разработка (Integration)** | Основная ветка для интеграции всех новых функций. Все новые фичи сливаются сюда. Эта ветка должна быть всегда в рабочем состоянии, но может содержать незавершенный контент. |
| `feature/<название>` | **Ветки функций** | Создаются от `develop`. Используются для разработки одной конкретной новой функции (например, `feature/combat-system`, `feature/inventory-ui`). После завершения сливаются обратно в `develop` через Pull Request. |
| `hotfix/<название>` | **Срочные исправления** | Создаются от `main`. Используются для быстрого исправления критических багов в релизных версиях. После исправления сливаются и в `main`, и в `develop`. |

## 🤝 Как начать работу

1.  Клонируйте репозиторий: `git clone [URL_РЕПОЗИТОРИЯ]`
2.  Перейдите в папку проекта: `cd SonMagnus`
3.  Переключитесь на основную ветку разработки: `git checkout develop`
4.  Создайте свою ветку для работы над задачей (см. [COMMIT_CONVENTION.md](#commit_conventionmd) и [PULL_REQUEST_TEMPLATE.md](#pull_request_templatemd)):
    `git checkout -b feature/название-вашей-фичи`

## 📝 Документация

*   [COMMIT_CONVENTION.md](COMMIT_CONVENTION.md) — Правила оформления коммитов.
*   [PULL_REQUEST_TEMPLATE.md](PULL_REQUEST_TEMPLATE.md) — Шаблон для создания запросов на слияние (Pull Requests).
*   [.gitignore](.gitignore) — Файл, исключающий ненужные файлы Godot из контроля версий.

